# 데이터 품질 현황

이 문서는 유물 메타데이터·이미지·원격 API 연동의 현재 상태와 릴리스 게이트를 기록합니다.

## 검증된 항목

- 유물 ID는 1부터 100까지 중복 없이 연속됩니다.
- 모든 레코드가 `ko`, `en`, `zh`, `ja`, `th` 텍스트 계약을 충족합니다.
- 카테고리와 전시관은 정본 enum을 사용합니다.
- 데이터에서 참조하는 100개 이미지 경로는 모두 파일시스템에 존재합니다.
- 승인 상품의 코드·목록·상세·이미지·오류 계약을 기술문서와 대조했습니다.
- eMuseum client는 HTTPS를 기본값으로 사용하고, 명시적으로 승인한 경우에만 정확한 legacy HTTP origin을 허용합니다. 단일 key encoding, no-store, redirect 거부, timeout·응답 크기 제한도 적용합니다.
- 파싱 결과·candidate·snapshot에는 `serviceKey`와 키가 포함된 이미지 URL이 남지 않습니다.
- 검증 snapshot과 이미지 SHA-256·권리 manifest를 검사하는 릴리스 게이트를 추가했습니다.

## 아직 해결하지 못한 내용

### 1. 유물과 이미지 연결

100개 레코드가 60개의 고유 이미지 경로만 참조합니다. 일부 연결은 파일명과 유물명이 다릅니다. 예를 들어 신라 황금보검, 조선 일월오봉도, 고구려 강서대묘 벽화 레코드는 현재 서로 다른 유물을 가리키는 파일명을 사용합니다.

`public/artworks` 안에는 `* 2.jpg` 형태의 바이트 단위 동일 복사본이 81개, 약 229MB 있습니다. 현재 데이터는 이 복사본을 참조하지 않고 `.vercelignore`가 배포 업로드에서 제외합니다. 저장소에서의 물리적 삭제는 별도 바이너리 정리 작업으로 남아 있습니다.

### 2. 공식 ID·권리 검토 미완료

사용 상품은 다음 REST 상품으로 확정되었습니다.

- [문화체육관광부 국립중앙박물관_전국 박물관 유물정보 상세설명](https://www.data.go.kr/data/3036708/openapi.do)
- legacy base URL: `http://www.emuseum.go.kr/openapi`
- 기능: 코드 목록, 소장품 목록, 소장품 상세
- 일일 한도: 1,000회

사용자가 평문 HTTP 전송 위험을 승인한 뒤, 인증키를 커밋되지 않는 `.env.local`에서만 읽어 실제 후보 감사를 실행했습니다. 같은 키로 HTTPS 호출은 `4012`가 반환됐고 legacy HTTP 호출은 정상 코드 `0000`을 반환했습니다.

2026-07-11에 로컬 명칭 100건을 각각 한 번씩 조회한 감사 결과는 다음과 같습니다.

- 자동 확정하지 않고 수동 검토로 남긴 항목: 91건
- 유일한 exact-name 후보가 나온 항목: 6건
- API HTTP 500으로 격리한 항목: 3건(내부 ID 50, 69, 100)
- 전체 후보가 0건인 항목: 64건
- exact-name 후보가 여러 개인 항목: 13건

유일한 명칭 일치만으로는 동일 유물을 증명할 수 없습니다. 실제 유물번호 표본에서도 `본관13972`는 공식 명칭이 `산수무늬 벽돌`, `본관6255`는 `경주 부부총 금귀걸이`로 확인되어 현재의 일반화된 로컬 명칭과 차이가 있었습니다. 따라서 기존 100개 내부 레코드와 공식 source ID의 연결, 이미지별 제3자 권리, 출처표시 문구는 아직 사람의 최종 검토가 필요합니다. 현재 `data/generated/emuseum-artifacts.json`은 의도적으로 비어 있으며 `verified: false`입니다.

## 원격 API 연동 게이트

1. 공공데이터포털의 활용신청 상세에서 API 상품명과 승인 상태를 확인합니다.
2. 채팅·메일·이슈에 노출한 key는 운영 사용 전에 폐기하고 새 key를 발급합니다.
3. key는 로컬 `.env.local`에만 `MUSEUM_API_KEY`로 저장하고 배포 환경에는 전달하지 않습니다.
4. `npm run emuseum:discover -- --limit 5`로 소량 후보부터 확인합니다.
5. 공식 source ID를 사람이 검토한 뒤 `npm run emuseum:inspect`로 상세·이미지 ID를 확인합니다.
6. 메타데이터와 각 이미지의 권리 근거를 분리해 기록합니다.
7. `npm run emuseum:sync -- --dry-run`으로 고정 origin, MIME, magic bytes, 크기, SHA-256을 검증합니다. legacy HTTP는 위험 승인과 `MUSEUM_API_ALLOW_INSECURE_HTTP=true`가 모두 필요한 예외입니다.
8. 100건 전체를 검증한 뒤 snapshot을 게시하고 `npm run verify:emuseum`을 실행합니다.
9. snapshot gate와 권리 표시 회귀 검증 후에만 운영 환경의 `MUSEUM_DATA_VERIFIED=true`를 설정합니다.

상세 절차와 selection schema는 [EMUSEUM_SYNC.md](./EMUSEUM_SYNC.md)를 따릅니다.

## 이용 권리

전국 박물관 REST 상품은 공공데이터포털에 출처표시 조건과 제3자 권리 포함 가능성을 함께 안내합니다. 데이터셋의 공공누리 제1유형을 개별 이미지에 자동 적용하지 않습니다. 검증된 출처·라이선스·이미지 크레딧은 상세 화면, JSON-LD, metadata와 즐겨찾기 export에 유지됩니다.
